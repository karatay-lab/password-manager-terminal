# Rust build artifacts
/target
**/*.rs.bk
Cargo.lock.orig

# Environment / secrets — never commit
.env
.env.*
!.env.example

# Local reference docs (kept out of version control by request)
/docs/

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store

# Claude Code session transcript dumps (date-prefixed .txt at repo root)
/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt
